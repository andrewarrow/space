cd wasm
GOOS=js GOARCH=wasm go build -ldflags="-s -w -X main.useLive=true" -o ../web/assets/other/json.wasm 
cd ../web/assets/other
rm json.wasm.gz
gzip json.wasm
cd ../../
#tailwindcss -i assets/css/tail.components.css -o assets/css/tail.min.css --minify
DATABASE_URL=postgres://fred3:fred3@localhost/plot2?sslmode=disable
uuid=$(uuidgen); go build -ldflags="-X main.buildTag=$uuid" -o space-web
echo "3"
./space-web run 3000
